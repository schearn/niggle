.DS_Store
.claude/

# Internal handover notes — local only, never publish (public repo)
CONTEXT.md

# Case-study source images — the optimised web copies live in casestudies/
Niggle Casestudies/
courtcraft-case-study.jpg

# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
